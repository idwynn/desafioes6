import Cliente from './client.js';
import Impuesto from './impuesto.js';

let nombre = prompt("Dame el nombre del cliente0".valor);
let impuesto11 = prompt("Dame el monto bruto anual".valor);
let deducciones = prompt ("Dame el monto de las deducciones".valor)

console.log(`El impuesto  a pagar por el cliente es: ${cliente.calcularImpuestoTotal()}`);